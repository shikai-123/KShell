1、要用中文回答我，要是中文不方便，再用英文。
2、对于代码的开发方案，一定不要重复造轮子，除非单独造轮子有优势，你在造轮子之前，一定要征求我的同意。
3、代码一定要清晰明了，不要用偏门的方法。优先采用成熟的方案。
4、当你有不明白的地方，一定要问我，保证你和我的思路是统一的。比如我的提示词说的不够细致的之后，你一定要问明白之后，才能认可。

5、代码风格规范：使用 4 空格缩进；类名 PascalCase；函数名 camelCase；成员变量 m_ 前缀；全局变量 g_ 前缀。
6、Qt 开发规范：优先使用 Qt 自带类；使用信号槽机制；内存管理用智能指针或父子对象机制；统一使用 QString。
7、错误处理：使用 Qt 日志系统（qDebug/qWarning/qCritical）；关键操作必须有错误处理；用户可见错误要友好提示。
8、第三方库使用规范：引入新的第三方库前必须征求我的同意；选择第三方库时优先考虑成熟、稳定、社区活跃的库；如果现有库不能满足需求，可以提出更换方案，但需要说明理由。
9、文件组织：代码放 KShell/；UI 用 Qt Designer；资源放 img/ qss/；配置放 conf/。
10、测试调试：修改后确保编译通过；新功能要基本测试。
11、提交规范：格式 <类型>: <描述>；类型：feat(新功能)、fix(修复)、refactor(重构)、docs(文档)、style(格式)。
12、当我说有bug的时候，你要检查一下我的说bug是否真的存在，如果真的存在，才去修改代码。